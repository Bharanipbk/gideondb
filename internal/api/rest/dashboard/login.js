const form=document.getElementById("login-form"),notice=document.getElementById("login-error");
form.addEventListener("submit",async event=>{event.preventDefault();notice.classList.add("hidden");const button=form.querySelector("button");button.disabled=true;try{const response=await fetch("/v1/dashboard/session",{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify({username:form.username.value,password:form.password.value})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.message||"Sign in failed");sessionStorage.setItem("gideondb-dashboard-csrf",body.csrf_token);location.replace("/dashboard/")}catch(error){notice.textContent=error.message;notice.classList.remove("hidden")}finally{button.disabled=false}});
