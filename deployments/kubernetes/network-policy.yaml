apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vectordb-ingress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: vectordb
  policyTypes: ["Ingress"]
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 6333
    - from:
        - namespaceSelector:
            matchLabels:
              vectordb-client-access: "true"
      ports:
        - protocol: TCP
          port: 6333

